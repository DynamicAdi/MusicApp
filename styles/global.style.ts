import { StyleSheet } from "react-native";

export const global = StyleSheet.create({
    container: {
        paddingHorizontal: 12,
        paddingRight: 0
    },
    player: {
        position: "absolute",
        right: 10,
        bottom: 15,
    }
})