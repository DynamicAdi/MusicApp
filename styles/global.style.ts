import { StyleSheet } from "react-native";

export const global = StyleSheet.create({
    container: {
        paddingHorizontal: 12,
        paddingRight: 0
    },
    player: {
        position: "absolute",
        left: 10,
        // right: 0,
        bottom: 15,
    }
})